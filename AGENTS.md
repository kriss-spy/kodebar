## Agent skills

### Issue tracker

GitHub issues for `kriss-spy/kodebar`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context — `backend/` (native Linux provider-probe service) and `frontend/` (KDE Plasmoid). See `docs/agents/domain.md`.
